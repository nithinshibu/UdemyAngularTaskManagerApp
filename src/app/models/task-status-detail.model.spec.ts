import { TaskStatusDetail } from './task-status-detail.model';

describe('TaskStatusDetail', () => {
  it('should create an instance', () => {
    expect(new TaskStatusDetail()).toBeTruthy();
  });
});
